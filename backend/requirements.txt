# Python 3.11 / 3.12 deployment requirements
#
# IfcOpenShell now ships official wheels on PyPI for Python 3.11 / 3.12,
# so we install it directly (no runtime binary download needed).

# Web framework
fastapi==0.115.6
uvicorn==0.30.6
httpx==0.27.2
python-multipart==0.0.12
aiofiles==24.1.0

# Real parsing engines
ifcopenshell==0.8.1.post1
ezdxf==1.3.5
pdfplumber==0.11.4
