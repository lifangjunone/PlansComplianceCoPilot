# Development-only Vite proxy target for requests whose path starts with /api.
# Replace this with the domain of the deployed backend service when applicable.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
